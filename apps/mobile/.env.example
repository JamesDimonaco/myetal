# Where the mobile app talks to the backend.
# Use http://10.0.2.2:8000 for the Android emulator (it maps to the host's localhost).
# Use http://localhost:8000 for the iOS simulator.
# Use https://api.myetal.app for production.
EXPO_PUBLIC_API_URL=http://localhost:8000

# PostHog analytics — never commit actual values
EXPO_PUBLIC_POSTHOG_KEY=
EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
